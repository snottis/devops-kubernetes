apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: img-claim
  namespace: project
spec:
  storageClassName: img-manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500M