apiVersion: v1
kind: Service
metadata:
  name: pingpong-svc
spec:
  type: ClusterIP
  selector:
    app: pingpong
  ports:
    - port: 2345
      name: default
      protocol: TCP
      targetPort: 3000
    - port: 80
      name: http
      protocol: TCP
      targetPort: 3000